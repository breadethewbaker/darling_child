// The superclass for all person entities in the game
#include <stdlib.h>
#include <iostream>

class Person() {

  Person (char f[], char l[], int a) {
    _age = a;
    _firstN = f[];
    _lastN = l[];

    Public:
      std.out(_age);
  }
}
